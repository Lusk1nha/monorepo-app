[package]
name = "todos"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { workspace = true, features = ["full"] }

sqlx = { version = "0.8.3", features = [
  "runtime-tokio-rustls",
  "macros",
  "postgres",
  "chrono",
] }
